import type { ProjectContent } from "../../types"

export const trailhead = {
  blurb:
    "Offline hiking tracker for iOS and Android. Background GPS recording, vector map tiles cached on device, and a route export that survives a day with no signal.",
} as const satisfies ProjectContent
